-- Migração inicial do sistema TPM
-- Todas as tabelas devem ter o prefixo TPM_

-- Este arquivo está preparado para receber as definições de tabelas
-- conforme o sistema for desenvolvido.

-- Exemplo de estrutura (comentado):
-- CREATE TABLE IF NOT EXISTS TPM_exemplo (
--   id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
--   created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,
--   updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL
-- );

-- Adicione suas tabelas abaixo seguindo o padrão TPM_nome_da_tabela
